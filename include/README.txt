This directory contains third-party libraries, especially of the header-only kind.

As a rule, we try not to check third-party libraries into the repository, but use system packages instead, but those do not exist for small header-only libraries.
